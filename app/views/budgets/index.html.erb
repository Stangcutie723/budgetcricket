<h1>Budget Tracker</h2>
<h3>Add your budgets and track expenses!</h3>

<ul>
  <% @budgets.each do |budget| %>
    <li><%= link_to budget.name, budget %></li>
  <% end %>
</ul>

<%= link_to "New Budget", new_budget_path %>
